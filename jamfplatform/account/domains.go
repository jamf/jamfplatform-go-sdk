// Code generated by tools/generate; DO NOT EDIT.

// Copyright Jamf Software LLC 2026
// SPDX-License-Identifier: MIT

package account

import (
	"context"
	"fmt"
	"net/http"
	"net/url"

	"github.com/jamf/jamfplatform-go-sdk/internal/client"
)

// ListDomains list Domains.
//
// Required privileges: sso-domains:read.
// The published spec declares none for this operation; these are the
// capabilities the gateway's own authorization policy enforces. See
// Privileges in this package for the provenance.
func (c *Client) ListDomains(ctx context.Context) ([]Domain, error) {
	prefix := c.transport.APIPrefix("sso", "v1")
	endpoint := prefix + "/domains"

	results, err := client.UnwrapResults[Domain](ctx, c.transport, http.MethodGet, endpoint, "results")
	if err != nil {
		return nil, fmt.Errorf("ListDomains: %w", err)
	}
	return results, nil
}

// CreateDomain add Domain.
//
// Required privileges: sso-domains:create.
// The published spec declares none for this operation; these are the
// capabilities the gateway's own authorization policy enforces. See
// Privileges in this package for the provenance.
func (c *Client) CreateDomain(ctx context.Context, request *DomainRequest) (*Domain, error) {
	prefix := c.transport.APIPrefix("sso", "v1")
	var result Domain
	endpoint := prefix + "/domains"
	if err := c.transport.DoWithContentType(ctx, http.MethodPost, endpoint, request, "application/json", http.StatusCreated, &result); err != nil {
		return nil, fmt.Errorf("CreateDomain: %w", err)
	}
	return &result, nil
}

// GetDomainAllocation get Domain Allocation.
//
// Required privileges: sso-domains:read.
// The published spec declares none for this operation; these are the
// capabilities the gateway's own authorization policy enforces. See
// Privileges in this package for the provenance.
//
// Parameters:
//   - domain: The domain name itself, as returned in the `domain` field of a domain. Domains have no separate
//     opaque identifier for allocation lookups.
func (c *Client) GetDomainAllocation(ctx context.Context, domain string) (*DomainAllocation, error) {
	prefix := c.transport.APIPrefix("sso", "v1")
	var result DomainAllocation
	endpoint := fmt.Sprintf("%s/domains/allocation/%s", prefix, url.PathEscape(domain))
	if err := c.transport.Do(ctx, http.MethodGet, endpoint, nil, &result); err != nil {
		return nil, fmt.Errorf("GetDomainAllocation(%s): %w", domain, err)
	}
	return &result, nil
}

// DeleteDomain delete Domain.
//
// Required privileges: sso-domains:delete.
// The published spec declares none for this operation; these are the
// capabilities the gateway's own authorization policy enforces. See
// Privileges in this package for the provenance.
//
// Parameters:
//   - domainID: Jamf-assigned identifier of the domain, as returned in the `id` field of a domain. Treat it as an
//     opaque string: it is currently a decimal number, and a value that is not one is rejected with `400`.
func (c *Client) DeleteDomain(ctx context.Context, domainID string) error {
	prefix := c.transport.APIPrefix("sso", "v1")
	endpoint := fmt.Sprintf("%s/domains/%s", prefix, url.PathEscape(domainID))
	if err := c.transport.DoExpect(ctx, http.MethodDelete, endpoint, nil, http.StatusNoContent, nil); err != nil {
		return fmt.Errorf("DeleteDomain(%s): %w", domainID, err)
	}
	return nil
}

// VerifyDomain verify Domain.
//
// Required privileges: sso-domains:update.
// The published spec declares none for this operation; these are the
// capabilities the gateway's own authorization policy enforces. See
// Privileges in this package for the provenance.
//
// Parameters:
//   - domainID: Jamf-assigned identifier of the domain, as returned in the `id` field of a domain. Treat it as an
//     opaque string: it is currently a decimal number, and a value that is not one is rejected with `400`.
func (c *Client) VerifyDomain(ctx context.Context, domainID string) (*Domain, error) {
	prefix := c.transport.APIPrefix("sso", "v1")
	var result Domain
	endpoint := fmt.Sprintf("%s/domains/%s/actions/verify", prefix, url.PathEscape(domainID))
	if err := c.transport.Do(ctx, http.MethodPost, endpoint, nil, &result); err != nil {
		return nil, fmt.Errorf("VerifyDomain(%s): %w", domainID, err)
	}
	return &result, nil
}
